interface:
  display_name: "Lens Testing"
  short_description: "Add risk-based Lens HTTP contract coverage"
  default_prompt: "Use $lens-testing to decide whether this change needs backend HTTP contract coverage, then write the smallest API-level test through the shared fixtures and run the relevant uv-managed checks."
